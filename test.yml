---
- name: Check nginx
  command: "systemctl status nginx.service"
  register: service_state

- name: check service
  fail:
    when: service_state

- name: Uri
  uri:
    url: "http://localhost:{{ nginx_port }}"
    status_code: 404
    timeout: 1
